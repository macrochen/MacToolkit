@@ -160,7 +160,7 @@
         }
         
         // 三指触摸中 → 拖动
-        if activeFingerCount >= 3 && state == 1 && !isDragging {
+        if activeFingerCount >= 3 && !isDragging {
             isDragging = true
             let cursorPos = CGEvent.mouseLocation()
             onThreeFingerDown?(CGPoint(x: cursorPos.x, y: cursorPos.y))
@@ -168,7 +168,7 @@
         }
         
         // 拖动中 → 移动
-        if isDragging && state == 1 {
+        if isDragging {
             let cursorPos = CGEvent.mouseLocation()
             onThreeFingerMove?(CGPoint(x: cursorPos.x, y: cursorPos.y))
             return
